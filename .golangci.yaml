version: "2"

linters:
    settings:
        errcheck:
            check-type-assertions: true
            exclude-functions:
                - (io.Closer).Close

formatters:
    enable:
        - goimports
        - golines

